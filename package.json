{
  "name": "web-dev-server-lit",
  "version": "0.0.1",
  "main": "index.js",
  "author": "Ming-der Wang",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "build:watch": "npm run build -- --watch",
    "serve": "web-dev-server --node-resolve --watch --open",
    "dev": "npm run build:watch & npm run serve"
  },
  "devDependencies": {
    "@web/dev-server": "^0.1.31",
    "typescript": "^4.6.4"
  },
  "dependencies": {
    "lit": "^2.2.3"
  }
}
